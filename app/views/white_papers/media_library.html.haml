.margin-top
  -if flash[:success] || flash[:error] || flash[:warning]
    =render partial: 'layouts/flash_messages'
.l-section-small.is-bg-asphalt
  .l-wrap
    .container-fluid
      .row.l-margin-bottom
        .col-md-12.text-center
          %h1.l-margin-bottom.l-margin-top.text-center.h-separator.h-separator-center{style: 'letter-spacing:1.5px;'}
            =t('views.white_papers.index.h1')
.l-section-small.is-bg-asphalt
  .l-wrap
    .container-fluid
      .col-md-12
        .table-box
          -@white_papers.each do |paper|
            .col-sm-6.col-md-3.col-lg-3
              .box
                =link_to public_white_paper_url(paper.try(:name_url)), class: 'box box-profile' do
                  .box-inner
                    =image_tag paper.try(:cover_image).try(:url), class: 'media-pic'
                    %p
                      =paper.try(:name)



.l-section-small.is-bg-asphalt
  .l-wrap
    .container-fluid
      .row.l-margin-bottom
        .col-md-12.text-center
          %h1.l-margin-bottom.l-margin-top.text-center.h-separator.h-separator-center{style: 'letter-spacing:1.5px;'}
            =t('views.mock_exams.index.h1')
.l-section-small.is-bg-asphalt
  .l-wrap
    .container-fluid
      .col-md-12
        .table-box
          -@mock_exams.each do |mock_exam|
            .col-sm-6.col-md-3.col-lg-3
              .box
                -if current_user
                  =link_to mock_exam_url(mock_exam), class: 'box box-profile' do
                    .box-inner
                      =image_tag mock_exam.try(:cover_image).try(:url), class: 'media-pic'
                      %p
                        ='Purchase'
                        =mock_exam.try(:name)
                -else
                  =link_to sign_in_url(flash: :true), class: 'box box-profile' do
                    .box-inner
                      =image_tag mock_exam.try(:cover_image).try(:url), class: 'media-pic'
                      %p
                        ='Purchase'
                        =mock_exam.try(:name)

