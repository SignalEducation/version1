.modal.fade{id: 'confirm-pause-modal', tabindex: '-1', role: 'dialog', 'aria-labelledby': 'confirmPauseModal', 'aria-hidden': 'true'}
  .modal-dialog{role: 'document'}
    .modal-content
      %button.btn.btn-icon.modal-close{'data-dismiss': 'modal'}
        %span
          Close
        %i.material-icons.ml-1{'aria-hidden': 'true'}
          close
      .modal-header
        %h3{id: 'confirmPauseModal'}
          ='PAUSE ACCOUNT'

      .modal-body
        .row
          .col-sm-12
            %p.h4
              We understand that students sometimes take a break between exams. That’s why we’ve set up the pause function.
            %p.h4
              Your billing will be paused, then when you resume your subscription, you’ll secure your current low price when you return to study for your next exam.

      .col-sm-6
        =link_to subscriptions_suspensions_path(id: @subscription.id), method: :post do
          .btn.btn-orange.full-width.l-margin-bottom
            =t('views.users.show.confirm_subscription_pause')


