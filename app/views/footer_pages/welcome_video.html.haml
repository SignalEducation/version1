.hero-library
  .l-wrap-small
    =render partial: 'layouts/transparent_navbar'
    .l-section-footer-pages
      .container-fluid
        .row
          .col-md-8.col-md-offset-2
            %h1.h1.h-white.h-separator.h-separator-center.text-center.l-margin-bottom
              Introduction Welcome Video

-if flash[:success] || flash[:error] || flash[:warning]
  =render partial: 'layouts/flash_messages'
.l-asphalt
  .l-section
    .l-wrap-small
      .container-fluid
        .row
          .col-md-10.col-md-offset-1
            .video
              .play
                #vimeo-player.vimeo-player




            .l-margin-top-huge
              %p.text-center
                If you have any queries you can write to us at:
            .col-sm-12.text-center
              %p
                =mail_to 'info@learnsignal.com' do
                  .strong
                    info@learnsignal.com


:javascript

  var player = new Vimeo.Player('vimeo-player', {id: '250643235'});

  $(document).ready(function(){
    player.play();
  });

