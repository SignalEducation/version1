.container
  %header.management-console-section
    %h1
      General Reports

  %section.pb-7
    .col-sm.pt-4
      .row
        .col-sm-6
          = link_to 'Total Users', export_users_path(format: :xls), class: 'btn btn-primary'
        .col-sm-6
          = link_to 'Courses List', export_courses_path(format: :xls), class: 'btn btn-secondary'

    .col-sm.pt-4
      .row
        .col-sm-6
          =link_to 'Monthly Users', export_users_monthly_path(format: :xls), class: 'btn btn-primary '

        .col-sm-6
          =link_to 'Users with Enrolments', export_users_with_enrollments_path(format: :xls), class: 'btn btn-secondary'

    .col-sm.pt-4
      .row
        .col-sm-6
          =link_to 'Users with UTM data', export_visits_path(format: :xls), class: 'btn btn-secondary'
        .col-sm-6
          -#=link_to 'Enrolments', export_enrollments_path(format: :xls), class: 'btn btn-cyan btn-wide'




