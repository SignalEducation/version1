<% if @levels.present? %>
  $('#key_area_level_id').html("<%= j options_from_collection_for_select(@levels, :id, :name) %>");
<% else %>
  $('#key_area_level_id').html("<%= j options_from_collection_for_select([]) %>");
<% end%>
