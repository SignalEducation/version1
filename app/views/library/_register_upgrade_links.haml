-if user && !user.can_view_content?(@group.exam_body_id)
  =link_to subscription_checkout_special_link(@course.exam_body_id), class: 'btn btn-primary btn-sm mb-5' do
    Upgrade Now
-elsif !user
  =link_to new_student_url, class: 'btn btn-primary btn-sm mb-5' do
    Register Now