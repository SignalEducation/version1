=form_for(@user_session, html: {style: 'min-height: 600px;'}) do |f|

  .bg-white.p-3.px-sm-5.pt-sm-4
    .py-3
      -if @home_page && @home_page.login_form_heading && !@home_page.login_form_heading.empty?
        =@home_page.login_form_heading
      -else
        Explore our innovative learning platform and discover how learnsignal helps you pass your exams faster
      =render partial: '/layouts/error_messages', locals: {thing: @user_session, message: t('views.user_sessions.form.error_message')}

      %fieldset.py-3
        %legend.sr-only
          Log in Details

        .row
          .col-sm-12
            .form-group
              =f.label :email, t('views.user_sessions.form.email')
              =f.email_field :email, placeholder: 'Enter your email here', class: 'form-control'

          .col-sm-12
            .form-group
              =f.label :password, t('views.user_sessions.form.password')
              =f.password_field :password, placeholder: 'Enter your password here', class: 'form-control'


      .d-flex.justify-content-between.align-items-center.pt-3
        =f.submit t('views.general.sign_in'), class: 'btn btn-primary btn-sm mb-4'


        .logo-acca
          -if @home_page && @home_page.logo_image && @home_page.logo_image.present?
            =#image_tag(@home_page.logo_image, class: 'logo-acca', alt: 'Logo')
            =image_tag('learning-partner-badge.png', alt: 'Logo')

