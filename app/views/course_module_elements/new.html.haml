.container
  %header.management-console-section
    %h1
      -if @course_module_element.is_video
        =t('views.course_module_elements.new.video')
      -elsif @course_module_element.is_quiz
        =t('views.course_module_elements.new.quiz')
      -elsif @course_module_element.is_constructed_response
        =t('views.course_module_elements.new.constructed_response')
      -else
        =t('views.course_module_elements.new.h1')

  %section.pb-7
    =render partial: 'form'
