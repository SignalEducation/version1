.l-section-small.is-bg-asphalt{style: 'padding-left: 30px; padding-right: 30px;'}
  .container-fluid.l-padding-side-3x
    .row.l-margin-bottom.l-margin-top
      .col-md-12.col-sm-12.col-xs-12.text-center
        %h1.h-separator.h-separator-center{style: 'letter-spacing:1.5px;'}
          =t('views.subject_course_resources.index.h1')

      .col-md-12.l-margin-top-big
        .corporate-box-item.table-box
          .table-responsive
            %table.table.sorted_table{style: 'font-size: 18px;', data: {destination: subject_course_resources_url}}
              %thead
                %tr
                  %th=t('views.subject_course_resources.form.name')
                  %th=t('views.subject_course_resources.form.subject_course_id')
                  %th='Active'
                  %th
              %tbody
                -@subject_course_resources.each do |subject_course_resource|
                  %tr{id: subject_course_resource.id}
                    %td=subject_course_resource.name
                    -if subject_course_resource.subject_course
                      %td=subject_course_resource.subject_course.name
                    %td=tick_or_cross(subject_course_resource.active)
                    %td
                      -if subject_course_resource.external_url
                        =link_to t('views.general.view'), subject_course_resource.external_url, target: '_blank', class: 'btn btn-purple btn-small'
                      -else
                        =link_to t('views.general.view'), subject_course_resource.file_upload.url, target: '_blank', class: 'btn btn-purple btn-small'
                      =link_to t('views.general.edit'), edit_subject_course_resource_url(subject_course_resource), class: 'btn btn-cyan btn-small'
                      -if subject_course_resource.destroyable?
                        =link_to t('views.general.delete'), subject_course_resource, method: :delete, data: { confirm: t('views.general.delete_confirmation') }, class: 'btn btn-red btn-small'
                    %td
                      %span.glyphicon.glyphicon-sort{title: t('views.general.drag_to_reorder')}

    .row
      =link_to t('views.general.new'), new_subject_course_resource_url, class: 'btn btn-red', title: t('views.general.new_tooltip')
