%main
  %article.bg-gray5
    .container
      %header.hero-section.hero-section-external
        %h1.h1-hero.mb-3
          Exercise Submission
        %p.p-hero.px-lg-8
          Submit a PDF of your exercise or mock exam below for correction.

      %section.pb-md-6.pb-5
        .forgot-password-section
          .form-box
            =form_for @exercise, {class: 'py-3 mr-5 ml-5', role: 'form'} do
              .row
                .col-sm-12
                  .form-group
                    -if f.object.present?
                      =f.label :submission, t('views.mock_exams.form.file'), class: 'control-label'
                      .form-control-static
                        =link_to f.object.submission_file_name, f.object.submission.url, target: '_blank'
                        =number_to_human_size(f.object.file_file_size)
                        %br/
                        =f.label :submission, t('views.white_papers.form.file_replacement'), class: 'control-label'
                    -else
                      =f.label :submission, t('views.mock_exams.form.file'), class: 'control-label'
                    .input-group.input-group-lg
                      =f.file_field :submission, class: 'form-control'
              .pt-3
                =f.submit 'Upload Submission', class: 'btn btn-primary btn-sm'