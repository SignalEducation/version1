.l-section-small.is-bg-asphalt
  .l-wrap-large
    .container-fluid
      .row.l-margin-bottom
        .col-md-12.text-center
          %h1.l-margin-bottom.text-center.h-separator.h-separator-center
            Users

      .row.l-margin-top.l-margin-bottom
        .col-md-12
          =render partial: 'management_consoles/user_management_nav'

      .row
        .col-sm-12
          .box-item.table-box
            .row.l-margin-bottom
              .col-xs-8
                %a{data: {remodal: {target: 'csv-upload-modal'}}, class: 'btn btn-primary btn-small'}
                  Bulk Invite New Users
                =link_to 'Invite New user', new_user_url, class: 'btn btn-primary btn-small'

              .col-xs-4
                =form_tag(search_users_url, role: 'form', class: 'form-inline input-group-lg') do
                  .form-group.input-group.input-group-lg
                    =text_field_tag :search_term, params[:search_term], class: 'form-control', placeholder: t('views.general.search'), onblur: 'this.form.submit(); return false;'
                    .input-group-addon{style: 'background-color: #EE5457; color: #fff;'}
                      %span.glyphicon.glyphicon-search

            .table-responsive
              %table.table{style: 'font-size: 18px;'}
                %thead
                  %tr
                    %th=t('views.users.form.full_name')
                    %th=t('views.users.form.email')
                    %th=t('views.users.form.email_verified')
                    %th=t('views.users.form.subscription_status')
                    %th
                    %th
                %tbody
                  -@users.each do |user|
                    %tr
                      %td=user.full_name
                      %td=user.email
                      %td=tick_or_cross(user.email_verified)
                      %td=user.user_account_status
                      %td
                        =link_to t('views.general.view'), user_url(user), class: 'btn btn-secondary btn-small'
                      %td
                        =link_to t('views.general.edit'), edit_user_url(user), class: 'btn btn-primary btn-small'

              .col-sm-10.user-pagination
                =will_paginate @users, renderer: BootstrapPagination::Rails


=render partial: 'users/csv_import_modal'
