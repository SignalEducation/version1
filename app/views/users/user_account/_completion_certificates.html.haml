.row
  .col-sm-12
    .table-box
      -if @certs.any?
        %table.table{style: 'font-size: 18px;'}
          %thead
            %tr
              %th
                =t('views.subject_course_user_log.show.course')
              %th
                =t('views.subject_course_user_log.show.completed_at')
              %th

          %tbody
            -@certs.each do |cert|
              %tr
                %td
                  =cert.subject_course.name.to_s.truncate(20)
                %td
                  =humanize_datetime(cert.updated_at)
                %td
                  =link_to completion_certs_url(cert.id, format: 'pdf'), target: '_blank' do
                    .btn.btn-purple.btn-small.pull-right
                      Download
