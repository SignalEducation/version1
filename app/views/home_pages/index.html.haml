=render partial: 'layouts/navbar'
.l-section-small.is-bg-asphalt{style: 'padding-left: 30px; padding-right: 30px;'}
  .container-fluid.l-padding-side-3x
    .row.l-margin-bottom.l-margin-top
      .col-md-12.col-sm-12.col-xs-12.text-center
        %h1.h-separator.h-separator-center{style: 'letter-spacing:1.5px;'}
          =t('views.home_pages.index.h1')
        -if flash[:success] || flash[:error] || flash[:warning]
          =render partial: 'layouts/flash_messages'

      .col-md-12.l-margin-top-big
        .corporate-box-item.table-box
          .table-responsive
            =will_paginate @home_pages, renderer: BootstrapPagination::Rails
            %table.table.sorted_table{style: 'font-size: 18px;'}
              %thead
                %tr
                  %th=t('views.home_pages.form.seo_title')
                  %th=t('views.home_pages.form.public_url')
                  %th=t('views.home_pages.form.custom_file_name')
                  %th
              %tbody
                -@home_pages.each do |home_page|
                  %tr
                    %td=home_page.seo_title
                    %td=home_page.public_url
                    %td=home_page.custom_file_name
                    %td
                      =link_to t('views.general.edit'), edit_home_page_url(home_page), class: 'btn btn-cyan btn-small'
                      -if home_page.destroyable?
                        =link_to t('views.general.delete'), home_page, method: :delete, data: { confirm: t('views.general.delete_confirmation') }, class: 'btn btn-red btn-small'

    .row
      .col-sm-12
        =link_to t('views.general.new'), new_home_page_url, class: 'btn btn-red', title: t('views.general.new_tooltip')

