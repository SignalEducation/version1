require 'rails_helper'

RSpec.describe "ExamSections", :type => :request do
  describe "GET /exam_sections" do
    xit "works! (now write some real specs)" do
      get exam_sections_path
      expect(response).to have_http_status(200)
    end
  end
end
