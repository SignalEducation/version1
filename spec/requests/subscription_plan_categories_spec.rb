require 'rails_helper'

RSpec.describe "SubscriptionPlanCategories", :type => :request do
  describe "GET /subscription_plan_categories" do
    xit "works! (now write some real specs)" do
      get subscription_plan_categories_path
      expect(response).to have_http_status(200)
    end
  end
end
